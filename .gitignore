# Secrets — never commit these
.env
.env.*
*.pem

# ...but the template must ship, or `cp .env.example .env` fails for everyone
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Cached embeddings and local scratch
poc/.cache/
*.npy

# OS
.DS_Store
